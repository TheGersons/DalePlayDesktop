<Page x:Class="StreamManager.Views.Pages.SuscripcionesPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
      Title="Suscripciones">

    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Título -->
        <TextBlock Grid.Row="0"
                   Text="Gestión de Suscripciones" 
                   Style="{StaticResource PageTitle}"/>

        <!-- Barra de acciones -->
        <StackPanel Grid.Row="1" 
                    Orientation="Horizontal" 
                    Margin="0,0,0,16">
            <Button x:Name="NuevoButton"
                    Style="{StaticResource MaterialDesignRaisedButton}"
                    Click="NuevoButton_Click"
                    Margin="0,0,8,0">
                <StackPanel Orientation="Horizontal">
                    <materialDesign:PackIcon Kind="AccountCash" 
                                            VerticalAlignment="Center"/>
                    <TextBlock Text="NUEVA SUSCRIPCIÓN" 
                              Margin="8,0,0,0"
                              VerticalAlignment="Center"/>
                </StackPanel>
            </Button>

            <Button x:Name="RefrescarButton"
                    Style="{StaticResource MaterialDesignOutlinedButton}"
                    Click="RefrescarButton_Click"
                    Margin="0,0,8,0">
                <StackPanel Orientation="Horizontal">
                    <materialDesign:PackIcon Kind="Refresh" 
                                            VerticalAlignment="Center"/>
                    <TextBlock Text="REFRESCAR" 
                              Margin="8,0,0,0"
                              VerticalAlignment="Center"/>
                </StackPanel>
            </Button>

            <!-- Filtros -->
            <ComboBox x:Name="EstadoFiltroComboBox"
                     materialDesign:HintAssist.Hint="Filtrar por estado"
                     Style="{StaticResource MaterialDesignOutlinedComboBox}"
                     MinWidth="150"
                     SelectionChanged="FiltroComboBox_SelectionChanged">
                <ComboBoxItem Content="Todos" Tag="" IsSelected="True"/>
                <ComboBoxItem Content="Activas" Tag="activa"/>
                <ComboBoxItem Content="Vencidas" Tag="vencida"/>
                <ComboBoxItem Content="Canceladas" Tag="cancelada"/>
            </ComboBox>
        </StackPanel>

        <!-- DataGrid de suscripciones -->
        <materialDesign:Card Grid.Row="2"
                             Padding="16"
                             materialDesign:ElevationAssist.Elevation="Dp2">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <!-- Buscador -->
                <TextBox x:Name="BuscarTextBox"
                         Grid.Row="0"
                         materialDesign:HintAssist.Hint="Buscar por cliente..."
                         Style="{StaticResource MaterialDesignOutlinedTextBox}"
                         Margin="0,0,0,16"
                         TextChanged="BuscarTextBox_TextChanged"/>

                <!-- DataGrid -->
                <DataGrid x:Name="SuscripcionesDataGrid"
                          Grid.Row="1"
                          AutoGenerateColumns="False"
                          CanUserAddRows="False"
                          CanUserDeleteRows="False"
                          IsReadOnly="True"
                          SelectionMode="Single"
                          materialDesign:DataGridAssist.CellPadding="8"
                          materialDesign:DataGridAssist.ColumnHeaderPadding="8">
                    <DataGrid.Columns>
                        <!-- Cliente -->
                        <DataGridTextColumn Header="Cliente" 
                                          Binding="{Binding ClienteNombre}"
                                          Width="*"/>

                        <!-- Perfil -->
                        <DataGridTextColumn Header="Perfil" 
                                          Binding="{Binding PerfilNombre}"
                                          Width="150"/>

                        <!-- Costo -->
                        <DataGridTextColumn Header="Costo Mensual" 
                                          Binding="{Binding CostoMensual, StringFormat=L {0:N2}}"
                                          Width="130"/>

                        <!-- Fecha Inicio -->
                        <DataGridTextColumn Header="Inicio" 
                                          Binding="{Binding FechaInicio, StringFormat=dd/MM/yyyy}"
                                          Width="110"/>

                        <!-- Próximo Pago -->
                        <DataGridTextColumn Header="Próximo Pago" 
                                          Binding="{Binding ProximoPago, StringFormat=dd/MM/yyyy}"
                                          Width="120"/>

                        <!-- Estado -->
                        <DataGridTemplateColumn Header="Estado" Width="100">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <Border Background="{Binding Estado, Converter={StaticResource EstadoColorConverter}}"
                                            CornerRadius="12"
                                            Padding="8,4">
                                        <TextBlock Text="{Binding Estado}" 
                                                  Foreground="White"
                                                  FontWeight="SemiBold"
                                                  HorizontalAlignment="Center"/>
                                    </Border>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>

                        <!-- Acciones -->
                        <DataGridTemplateColumn Header="Acciones" Width="220">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <StackPanel Orientation="Horizontal" 
                                               HorizontalAlignment="Center">
                                        <Button Style="{StaticResource MaterialDesignIconButton}"
                                               ToolTip="Ver detalles"
                                               Click="VerButton_Click"
                                               Tag="{Binding}">
                                            <materialDesign:PackIcon Kind="Eye"/>
                                        </Button>
                                        <Button Style="{StaticResource MaterialDesignIconButton}"
                                               ToolTip="Editar"
                                               Click="EditarButton_Click"
                                               Tag="{Binding}">
                                            <materialDesign:PackIcon Kind="Pencil"/>
                                        </Button>
                                        <Button Style="{StaticResource MaterialDesignIconButton}"
                                               ToolTip="Renovar"
                                               Click="RenovarButton_Click"
                                               Tag="{Binding}"
                                               Foreground="#4CAF50">
                                            <materialDesign:PackIcon Kind="Refresh"/>
                                        </Button>
                                        <Button Style="{StaticResource MaterialDesignIconButton}"
                                               ToolTip="Cancelar"
                                               Click="CancelarButton_Click"
                                               Tag="{Binding}"
                                               Foreground="Red">
                                            <materialDesign:PackIcon Kind="Cancel"/>
                                        </Button>
                                    </StackPanel>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>
                    </DataGrid.Columns>
                </DataGrid>

                <!-- Loading overlay -->
                <Grid x:Name="LoadingOverlay"
                      Grid.Row="1"
                      Background="#80000000"
                      Visibility="Collapsed">
                    <StackPanel VerticalAlignment="Center" 
                               HorizontalAlignment="Center">
                        <ProgressBar Style="{StaticResource MaterialDesignCircularProgressBar}"
                                    Value="0"
                                    IsIndeterminate="True"
                                    Width="48"
                                    Height="48"/>
                        <TextBlock Text="Cargando..." 
                                  Foreground="White"
                                  Margin="0,16,0,0"
                                  HorizontalAlignment="Center"/>
                    </StackPanel>
                </Grid>
            </Grid>
        </materialDesign:Card>
    </Grid>
</Page>
